import 'package:flutter/material.dart';

class SavedChatsViewBody extends StatelessWidget {
  const SavedChatsViewBody({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Saved Chats View"),
    );
  }
}
